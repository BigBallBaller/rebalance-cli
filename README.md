# rebalance-cli

A small Python CLI that generates a portfolio rebalance plan from a holdings CSV and target weights.

## Install
```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .